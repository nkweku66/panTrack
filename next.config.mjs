/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['imgs.search.brave.com', 'media.gettyimages.com','firebasestorage.googleapis.com']
    },
}
export default nextConfig;
